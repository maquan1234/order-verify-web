services:
  - type: web
    name: order-verify
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    healthCheckPath: "/"
    envVars:
      - key: DATA_DIR
        value: /data
